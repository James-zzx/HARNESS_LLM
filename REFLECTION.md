**反思报告**

此次智能化软件工程师训练营，是我第一次使用 $OpenCode + Superpowers + OpenDesign$ 组合进行基于 $AI$ 大语言模型的辅助编程软件开发。在此次开发过程中，我深刻体悟到了 $AI$ 辅助编程对于开发进程时间上的优化以及 $Superpowers$ 工作流对于开发过程的标准化，这让开发者能够在 $AI$ 的帮助下提升开发效率与开发质量，在 $AI$ 托管盛行的时代依旧能够保证不会松懈。

接下来我将从几个核心要点出发，具体地阐述我的反思与感受。

**一、哪些 $Superpowers$ 技能发挥了最大作用**

首先，$Superpowers$ 工作流中，`brainstorming` 和 `writing-plans`技能发挥了非常大的作用。作为整个工作流的第一步，这个技能将整个开发系统的点子整合起来。最初，我对 $Harness$ 的理解更多地来源于课程中老师讲授的知识，更多偏向于对概念的理解，缺少一个将概念具体化落实为一个真正的项目的过程。而当`brainstorming`技能通过几个核心问题，将我的思路一一整理成更有条理、有意义的文档时，我对软件开发乃至 $Harness$ 工程有了一个更加具象化的理解。一切思绪，从最初的无从下手，如填表格一般，逐渐转化为一个个具体的计划。

此外，该工作流中的每一个部分都发挥了一定的作用。形式上，它规范化了整个开发流程；实质上，一个个步骤都对整个开发流程有一定的推进效果。因而，我认为没有一个技能是完全“形式大于实质”的。

**二、$TDD$ 强制在 $AI$ 协作下是阻碍还是放大器？**

$TDD$ 强制在 $AI$ 协作下更多的是放大器。$TDD$ 通过红灯、绿灯、重构的循环，强制使得 $AI$ 在编写代码之前做强制性的测试，这样可以保证测试验证环节被严格执行了，进而保证了功能的正常运行能力。

不过 $TDD$ 也有一定的阻碍。这种先测试后写代码的模式在一定程度上降低了开发的效率（不过依然在可以接受的范围内）；这种模式还可能导致过度耦合，为了通过测试而“面向测试编程”，在后期重构时可能会触发大量的报错。

**三、$subagent-driven$ 工作流让智能体能自主运行多久而不偏离主题？**

在我开发过程中，$subagent-driven$ 工作流表现较好，可以保持在一个小时内基本不发生偏移。这也许与我对其额外加入的完成一个步骤时都需要及时汇报阶段性的结果的提示词有一定的关系。不过由于我给出的关于图形化界面的描述不够详细，工作流在前端的开发上出现了偏离，这让我在第一轮开发后追加了一轮修改与开发。

**四、什么样的 $task$ 颗粒度最优？**

根据此次项目实践得到的结果，我认为 $task$ 的颗粒度大概为一个基础的功能单元。
首先，项目采用偏模块化的设计，将一整个项目拆分为不同层级的不同模块，再通过 $worktrees$ 注意实现、落实。如果把过大的部分，比如一整个 $harness$ 或者是好几个功能一起划分为一个 $task$ 去做的话，实现效果相对较差，效率也相对更低一些。而当任务被拆分成多个小模块再逐一攻克之后，每一个功能的效果和独立性都有所提升。

**五、$SPEC / PLAN$ 质量如何影响实现质量（举一个"规约不清导致 'subagent' 偏离"的具体案例）？**

$SPEC / PLAN$ 质量对于实现质量的影响很大。

一个良好的 $SPEC / PLAN$ 文档，将每一个实现的功能与目标全面、具体但不啰嗦地呈现出来。全面地内容，降低了 $AI$ 自我编造的概率；不过长的文字描述，有尽量减小了 $AI$ 在原有开发道路上跑偏的可能。

举个例子：在初步进行开发时，我给出的对于 $UI$ 的描述过于简单，$AI$ 误以为将 $OpenDesign$ 作为一个插件而不是利用 $OpenDesign$ 为项目设计一个前端；当工作流进入到审查阶段时，我才发现这一问题，对其进行纠正。未经仔细打磨、推敲而直接进入开发环节，导致了负责这一部分的 $subagent$ 发生了较大的偏离。因而，$SPEC / PLAN$ 质量是非常重要的。

**六、你最有效的 $prompt / context$ 策略是什么、为什么有效？**

在原有的 $Superpowers$ 工作流的约束之外，我额外添加了一个 $prompt$，要求 $AI$ 在阅读文档或者是完成一个步骤时都需要及时汇报阶段性的结果。这样的多次汇报的模式，能够让我快速了解到当前的开发进展，进而进行监督，同时 $AI$ 的多次汇报也能够使得其偏离原有开发路线的可能性降低。此外，该 $prompt$ 还能够保障开发流程的流畅性。当 $AI$ 在阅读文档时，有时可能会因为一个敏感词而陷入到无限重试的死循环。当我额外加入这个提示词之后，死循环出现的频率大幅度降低了，并在一定程度上辅助我找出了发生错误的原因。

**七、凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？如果重做你会改变什么？**

这两条约束，让我思考清楚了许多问题。

$Superpowers$ 将 $API-Key$ 等凭据作了严格的规范约束，限制这类敏感信息在代码中明文出现，同时约束了 $AI$ 的权限。这其中带来的容易被忽略、被攻破的种种隐私泄露的问题与隐患，成功得到保障。

分发代表着任务上下文、计划文档、代码规范如何在“主控”与数十个“子代理”之间高效、准确地传递。分发的过程需要保证上下文的时效性，这让我了解到了一个问题———如果没有对过时的上下文的约束，子代理接受到任务后呈现的结果可能是过时的。

如果可以重做，我将会进一步做更详细的描述，在这两件事上进行强化，进一步提高代码的安全系数，协调开发过程中的时间逻辑。

**八、你对 $Superpowers$ 这套方法论的批判——它假设了什么，这些假设在你的项目里成立吗？**

$Superpowers$ 这套方法论，是基于流程对于提示词的约束可以提高工作质量的假设而展开的。它通过系统性的工作流流程，将 $AI$ 辅助的软件开发流程规范化为一个标准模型，通过将整个任务分解成有条理有逻辑的小部分来逐步攻克。这种开发模式，让我的项目思路更加清晰，具有模块化的形式。与不用这套模式的开发方式相比，最终呈现出的结果确实在质量上有所提高。因而这些假设在我的项目里可以被认为是的成立的。

以上，便是我这次开发 $Harness$ 模型的种种感悟。 $LLM$ 是引擎，$Harness$ 是车架，配上提示词的导航，方可绘就万卷诗篇。
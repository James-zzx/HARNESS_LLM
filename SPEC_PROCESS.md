# SPEC_PROCESS.md — 规约与计划生成过程记录

## 概述

本文档记录与 Superpowers 协作生成 SPEC.md 和 PLAN.md 的全过程，包括 brainstorming 关键节点、关键迭代决策、AI 建议的采纳与修正、以及对流程的反思。

## Brainstorming 关键节点

### 节点 1: 项目定位
智能体首先询问了项目的目标。我最初的想法是"对 LLM 进行限制，制造一个能够安全、自主、可分发地完成编码任务的机器人"。智能体将其拆解为四种可能的 Harness 方向（测试、开发调试、安全沙箱、工作流引擎），帮助我明确了方向为"安全沙箱 + 任务执行"。

### 节点 2: 技术栈选择
智能体提供了 Python / TypeScript / Rust 三种选项，并推荐了 Python。我选择了 Python，理由与其推荐一致：AI 生态最成熟，LLM SDK 支持最好。

### 节点 3: 功能模块的迭代扩展
最初我选择了 4 个核心功能模块。经过多轮迭代，智能体引导我逐步扩展到了 12 个模块，新增了:
- 工具/动作执行器（Action/Tool Executor）
- 主循环/决策引擎（Orchestrator）
- 治理护栏与 HITL 状态机
- 上下文与记忆管理
- 声明式配置系统
- 凭据安全存储
- 分发与 CI/CD
- Mock-LLM 抽象层

### 节点 4: Open Design 集成
智能体建议将 WebUI 从"最简 Jinja2 实现"改为集成 Open Design。我接受了这个建议，因为符合课程要求且能获得更专业的 UI 能力。

## 至少 3 轮关键迭代

### 迭代 1: 功能模块的增减与调整
**对话节选**: 智能体列出了 9 个模块，我要求增加 3 个新模块（凭据安全存储、分发与 CI/CD、Mock-LLM 抽象层）。
**我的决策**: 接受，因为凭据安全和可测试性是课程要求中的硬性约束。
**结果**: 模块从 9 个扩展到 12 个。

### 迭代 2: 技术方案的细化
**对话节选**: 智能体最初为模块 1 设计了"CLI + 最简 WebUI"，我要求改为"同时保留 CLI + REST API + 最简 WebUI"，并且模块 4 具体化为"运行 make test，抓取红/绿状态"。
**我的决策**: 接受，因为 REST API 是 WebUI 和未来集成的必要条件。
**结果**: 模块 1 支持三种接口，模块 4 的评估逻辑更加具体。

### 迭代 3: Open Design 集成
**对话节选**: 智能体在计划阶段询问是否需要对 SPEC.md 和 PLAN.md 进行调整，我要求将 Open Design 接入作为 WebUI 层。
**我的决策**: 接受，但要求智能体先调研 Open Design 的架构，确认其通过 HTTP API / MCP 协议可被 Python 项目集成后再做决定。
**结果**: 新增模块 13，替换了原有的"最简 WebUI"方案。PLAN.md 中的 P4-05 从"实现 Jinja2 模板"改为"实现 Open Design 集成层"。

## AI 建议的采纳与修正

### 采纳的建议
1. **技术栈选择**: 智能体推荐 Python + FastAPI + structlog 等技术栈，均被采纳。
2. **Open Design 集成**: 智能体建议将 WebUI 从自建改为集成 Open Design，采纳。
3. **Mock-LLM 抽象层**: 智能体建议加入 LLM Adapter 模式，确保可测试性，采纳。
4. **模块 4 的 make test 具体化**: 智能体在迭代中细化了评估逻辑，采纳。

### 修正/推翻的建议
1. **沙箱方案**: 智能体最初推荐 Docker 容器，我选择了 subprocess + 权限限制，因为更轻量且无需 Docker 依赖。
2. **任务分发方式**: 智能体最初推荐 REST API 作为主要方式，我坚持要求同时保留 CLI + REST API + WebUI 三种方式。
3. **凭据存储**: 智能体最初默认使用环境变量，我要求增加 OS 钥匙串支持，并禁止硬编码。

## 反思

### Brainstorming 做得好的地方
1. **追问深入**: 智能体能够在每个决策点追问"为什么"，帮助我澄清了许多模糊的假设。
2. **结构清晰**: 智能体能够将零散的需求自动归类为有结构的模块定义。
3. **渐进式细化**: 从 4 个功能到 12 个模块，智能体引导了渐进式的设计过程，而不是一次性抛出一个大方案。

### 让我不满的地方
1. **过于依赖选择题**: 智能体倾向于提供选择题而非开放式问题，有时限制了我的思考空间。
2. **对课程要求的理解不够**: 智能体最初没有主动检查 requirements.md 中的课程约束，需要我主动提示后才补充。
3. **迭代次数过多**: 从初始设计到最终确认经历了 7+ 轮迭代，部分原因是智能体在早期没有充分理解课程要求，导致后续需要反复修补。

### 对流程的反思
1. **SPEC.md 的完整性检查应该更早**: 智能体应该在 brainstorming 结束时主动检查 requirements.md 的 SPEC 要求清单，而不是等到我要求后才去做。
2. **PLAN.md 的 TDD 细节应该更早规范**: 初始 PLAN.md 中的验证步骤不够具体，需要在后续迭代中补充。